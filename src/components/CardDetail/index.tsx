export const CardDetail = ({text}: {text: string}) => {
    return (
        <div className="bg-brand-4 text-brand-1 w-14 rounded px-2 py-1 text-center text-sm font-inter font-500">
            {text}
        </div>
    )
}